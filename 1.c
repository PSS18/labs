
#include <stdio.h>
#include <math.h>

int main()
{
float a, x;
scanf("%f", &a);
scanf("%f", &x);
float G = ((4*(-4*pow(a,2)+a*x+5*pow(x,2)))/(-20*pow(a,2)+28*a*x+3*pow($
float F = (atan(24*pow(a,2)-25*a*x+6*pow(x,2)));
float Y = (log(2*pow(a,2)-7*a*x+6*pow(x,2)+1));
printf("G = %f \nF = %f \nY = %f \n", G, F, Y);
return 0;
}
